/**
 * Test case for benchmarkResources.
 * Runs with mocha.
 */
'use strict'

const benchmarkResources = require('../lib/cases/benchmark_resources.js')
const assert = require('assert')
const co = require('co')

describe('benchmark-resources', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Benchmark resources', () => co(function * () {

  }))
})

/* global describe, before, after, it */
