/**
 * Test case for benchmarkOne.
 * Runs with mocha.
 */
'use strict'

const benchmarkOne = require('../lib/cases/benchmark_one.js')
const assert = require('assert')
const co = require('co')

describe('benchmark-one', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Benchmark one', () => co(function * () {

  }))
})

/* global describe, before, after, it */
