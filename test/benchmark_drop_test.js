/**
 * Test case for benchmarkDrop.
 * Runs with mocha.
 */
'use strict'

const benchmarkDrop = require('../lib/cases/benchmark_drop.js')
const assert = require('assert')
const co = require('co')

describe('benchmark-drop', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Benchmark drop', () => co(function * () {

  }))
})

/* global describe, before, after, it */
