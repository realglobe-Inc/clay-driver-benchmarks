/**
 * Test case for benchmarkDestroy.
 * Runs with mocha.
 */
'use strict'

const benchmarkDestroy = require('../lib/cases/benchmark_destroy.js')
const assert = require('assert')
const co = require('co')

describe('benchmark-destroy', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Benchmark destroy', () => co(function * () {

  }))
})

/* global describe, before, after, it */
