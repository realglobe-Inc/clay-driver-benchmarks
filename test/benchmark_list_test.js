/**
 * Test case for benchmarkList.
 * Runs with mocha.
 */
'use strict'

const benchmarkList = require('../lib/cases/benchmark_list.js')
const assert = require('assert')
const co = require('co')

describe('benchmark-list', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Benchmark list', () => co(function * () {

  }))
})

/* global describe, before, after, it */
