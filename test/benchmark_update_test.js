/**
 * Test case for benchmarkUpdate.
 * Runs with mocha.
 */
'use strict'

const benchmarkUpdate = require('../lib/cases/benchmark_update.js')
const assert = require('assert')
const co = require('co')

describe('benchmark-update', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Benchmark update', () => co(function * () {

  }))
})

/* global describe, before, after, it */
